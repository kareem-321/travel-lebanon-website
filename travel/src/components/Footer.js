function Footer() {
  return (
    <footer>
      <div className="container">
        © {new Date().getFullYear()} Travel Lebanon — Made with React
      </div>
    </footer>
  );
}

export default Footer;
